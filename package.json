{
  "name": "thunderai-cf",
  "version": "1.0.0",
  "description": "ThunderAI — Cloudflare Worker with Groq backend",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
